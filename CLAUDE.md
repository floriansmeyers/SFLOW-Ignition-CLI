# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

SFLOW Ignition CLI — a new project (no source code yet). The repository name suggests a CLI tool related to SFLOW/Ignition.

## Allowed Tools

- WebSearch
- WebFetch
- Bash(npm run *)
- Bash(npx *)
- Bash(node *)
- Bash(git *)
- Bash(gh *)
- Bash(ls *)
- Bash(cat *)
- Bash(which *)
- Bash(echo *)
